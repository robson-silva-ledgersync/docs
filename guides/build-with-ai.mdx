---
title: "Connect with copy and paste"
description: "Hand one page to Claude or ChatGPT and let it build your bank connection for you. No coding by you."
---

Give the text on this page to Claude or ChatGPT and it writes your bank connection for you. You do not need to code or read any other page here.

You need your own software for the bank data to go into, and someone who can put the finished code into it. That is either the person who looks after it for you, or an AI tool that edits code directly, such as [Claude Code](https://claude.com/claude-code) or [Cursor](https://cursor.com). Claude and ChatGPT in a browser tab will write the code correctly, but they cannot install it for you.

Your own hands-on time is about 15 minutes. The waiting is us approving your account, and, if someone else looks after your software, them fitting it in.

## 1. Things only you can do

These three cannot be automated. Do them first, in order.

<Steps>
  <Step title="Sign up">
    Create an account at [portal.ledgersyncappv2.com](https://portal.ledgersyncappv2.com). Use a real work email address. Temporary ones are rejected.
  </Step>
  <Step title="Wait for us to approve you">
    Someone on our team checks every request by hand. There is no way to speed this up and no status page to watch. If a few days pass with no word, email [support@ledgersync.com](mailto:support@ledgersync.com).
  </Step>
  <Step title="Get your key">
    Once approved, sign in and open **Build → API keys** in the left menu. Create a key and choose **sandbox** for the environment. That is the practice setting: it uses fake bank data, so nothing you do can touch a real account.

    <Warning>
      Your key is shown **once**. Paste it into a note on your computer before you close the page. If you lose it, make a new one.
    </Warning>
  </Step>
</Steps>

## 2. Build the practice version

Open a **new** chat with Claude or ChatGPT, or with whichever AI tool edits your code. A fresh conversation works best.

<Steps>
  <Step title="Paste this in as your first message">
    You do not need to read any of it. It is written for the AI, not for you. Use the copy button in the top corner of the box.

````markdown paste-1-practice.md
# LedgerSync API: build my integration

Follow this document exactly. Do not invent endpoints, fields, query
parameters, or error codes.

I am not a programmer. Assume I cannot answer a technical question
without help.

If you need something only I can give you, ask me for one thing at a
time, in plain words, and explain what it is and where I would find it.
If I say I do not know, do not stop and do not guess: tell me the
options, pick a sensible default, and carry on.

You need three things from me. Ask for them one at a time:

- My API key. Assume it is a sandbox key unless I tell you otherwise.
- Where my app runs, so you know where to put the code. If I do not
  know, ask me who looks after my website and tell me to forward your
  answer to them.
- A public web address for receiving updates. If I do not have one, do
  not stall: set one up yourself if you can, or explain in one sentence
  what it is and offer to use a temporary tunnel for testing. Never just
  ask me for a URL and wait. `localhost` will never work.

When I give you my key, put it in my project's environment settings.
Never print it back to me and never write it into code you show me.

If you can fetch a web page yourself and need more detail than this
document carries, read https://docs.ledgersyncappv2.com/llms-full.txt

## What this API does

LedgerSync links my users' bank accounts and returns their accounts,
balances, transactions and statements through one REST API. Bank
passwords are typed into a LedgerSync-hosted page and never reach my
servers.

## Base URLs and auth

Sandbox: https://api-sandbox.ledgersyncappv2.com/v3
Live:    https://api.ledgersyncappv2.com/v3

Send `Authorization: Bearer <my key>` on every request. JSON over HTTPS.
There is no SDK, so use a plain HTTP client. A sandbox key does not work
against the live URL, or the reverse.

Every response has an `X-LS-Trace-Id` header. Log it.

## How the objects fit together

Client (`cli_` + 24 hex) is my record of one end user.
  -> Connection (`con_<SOURCE>_<number>`, e.g. `con_FINICITY_41294`),
     one per linked bank.
    -> Account (`acc_<SOURCE>_<number>`)
      -> Transaction (`txn_<SOURCE>_<number>`) and Statement
         (`stmt_<SOURCE>_<number>`)

SOURCE is FINICITY, MX, FDE or PDF, and LedgerSync picks it, never me.

## Build this flow

1. `POST /clients` with `{"name": "..."}`. Store the returned `cli_...` id.
2. `POST /clients/{client_id}/connect-session` with an empty body.
   Returns `{"url": "...", "expires_at": "..."}`.
3. Send that url to my end user. They open it, find their own bank, sign
   in, and choose accounts. I build no bank picker and never see their
   password.
4. Wait for the `connection.active` webhook. Read `data.connection.id`
   from it. That is the real connection id. Store it against my user.
5. `GET /accounts?client_id=cli_...&connection_id=con_FINICITY_41294`
6. `GET /accounts/{account_id}/transactions?client_id=cli_...&from=2026-01-01`

To receive webhooks, create a subscription once with
`POST /webhooks/subscriptions` with `{"url": "...", "event_types": ["*"]}`.
The response carries a `signing_secret` ONCE and no endpoint returns it
later. Do NOT echo it back to me. If you can write files, write it
straight into `.env` and tell me only that you did. If you cannot, stop
after the call and tell me to copy it out of the response myself. Never
repeat it in your reply, never put it in code you show me, never log it.

## Reading data

`GET /accounts?client_id=...` and optionally `&connection_id=...`
`GET /accounts/{account_id}/transactions?client_id=...&from=YYYY-MM-DD&to=YYYY-MM-DD`
   `from` includes that day, `to` excludes it. Newest first. Pending rows
   are always included, so branch on the `pending` field.
`GET /accounts/{account_id}/statements?client_id=...`
`GET /statements/{statement_id}/download?client_id=...` returns PDF bytes.

`client_id` is REQUIRED on every one of those. Leaving it off is a 400.

Transaction fields, and this is the complete list: `id`, `account_id`,
`external_id`, `amount`, `iso_currency_code`, `date`, `description`,
`merchant_name`, `category`, `pending`. `amount` is negative for money
out and positive for money in.

Account `type` is checking, savings, credit_card, loan, investment or
other. `current_balance` is from the account holder's point of view, so a
credit card with a balance owed comes back negative.

Lists page with `?cursor=...&limit=...` and return
`{"data": [...], "next_cursor": "...", "has_more": true}`. Keep passing
`next_cursor` back until `has_more` is false. A cursor only works for the
exact query that produced it.

Errors look like this. Branch on `error.code` and nothing else:

```json
{"error": {"code": "not_found", "message": "...", "type": "not_found",
           "category": "RESOURCE_NOT_FOUND", "trace_id": "..."}}
```

On a 429, honor the `Retry-After` header and back off.

## Verifying webhooks

Check every delivery before trusting it:

```
signed = X-LS-Webhook-Timestamp + "." + the raw request body
expected = hex(HMAC_SHA256(key = signing_secret, message = signed))
compare expected with the X-LS-Webhook-Signature header, constant-time
```

The key is the whole `whsec_...` string as UTF-8. Do not decode it.
Capture the raw body bytes before any JSON parsing touches them. Reject
anything whose timestamp is more than 5 minutes old.

Reply 2xx within about 10 seconds and queue anything slower: we drop the
connection after that and treat it as a failure. We retry for 24 hours,
so the same event can arrive twice. Ignore duplicates by `event_id`.

## Rules that will break my integration if you get them wrong

1. Only ever store a connection id shaped `con_<SOURCE>_<number>`. A
   `con_` followed by a plain uuid is a temporary placeholder and returns
   400 everywhere. The real id comes from the `connection.active`
   webhook, or from `GET /clients/{client_id}/connections`. Re-checking
   the operation never turns the placeholder into the real one.
2. `client_id` goes on every accounts, transactions and statements read.
3. Branch on `error.code`. Never on the HTTP status or the message text.
4. Sign `timestamp + "." + body`, never the body on its own.
5. Never automatically retry a failed POST. It creates a duplicate.
6. `external_id` and `metadata` on a Client come back empty on every
   read, so never build a lookup on them. Use the `cli_` id.
7. A statement `download_url` is a partial address. Join it to the API
   host, not to the `/v3` URL, and send my key with it.
8. The first webhook for a new connection is
   `connection.requires_action`. There is no `connection.initiated`.
9. There is no event for a newly opened account. Re-read `GET /accounts`
   and compare ids.
10. Never put my key in anything that runs in a browser or a phone app.

## Only if I ask you for my own bank picker

`GET /institutions?q=chase` finds an `ins_...` id, then
`POST /clients/{client_id}/connections` with `{"institution_id": "..."}`
returns a `202`. Read `GET /operations/{operation_id}` and open
`result.connection.action.widget_url` for the user. The real connection
id still only arrives on the webhook, exactly as in rule 1.

## Testing

Test bank: FinBank, `ins_0a01a5430925d0b2`. Inside the page, sign in with
username `demo` and password `go`. Use the sandbox URL and a sandbox key.
````
  </Step>

  <Step title="Say what you want, in your own words">
    Before you press send, add a sentence to the same message. Fill in the blank and do not worry about getting the words right:

    *"I want my clients' bank transactions to show up in ______. Give me a way to create a bank link for each client myself. Build the practice version first."*

    Now press send.
  </Step>

  <Step title="Answer its questions">
    It will ask you a few things, one at a time, in plain words. If you do not know an answer, say so. It has been told to explain the options and keep going rather than leave you stuck.

    <Warning>
      Give your key only when it asks for it, and only once. Never paste it in unprompted. If your key ends up displayed on screen, treat it as spent: make a new one in the portal and give it that one instead.
    </Warning>
  </Step>

  <Step title="Hand it to whoever looks after your software">
    Skip this step if your AI tool edits your code directly.

    When the AI says the code is ready, ask it: *"Write a short message I can forward to the person who looks after my software, explaining what to install."* Send them that message and a link to this page. Carry on once they tell you it is in.
  </Step>

  <Step title="Check it worked">
    Ask for the practice connection link, open it, type **FinBank** into the bank search, pick it, then sign in with username `demo` and password `go`.

    Made-up transactions should come back into your software. That is what success looks like.

    Nothing came back? Go to [section 4](#4-if-something-is-not-working).
  </Step>
</Steps>

## 3. Go live

Real bank accounts need a second approval. Same shape as before: a few things only you can do, then one more paste.

<Steps>
  <Step title="Start your live application">
    On the portal home page there is a panel about live access. Click **Request access**, or **Continue application** if you started one earlier.
  </Step>
  <Step title="Add a payment method">
    The wizard walks you through it. Paying is what submits your application, so an unpaid one is not waiting in a queue anywhere. You are not charged until you start using live bank data.
  </Step>
  <Step title="Wait for approval">
    A person reviews it, same as the first time.
  </Step>
  <Step title="Create your live key">
    Back to **Build → API keys**, create another key, and this time choose **live** for the environment. Shown once again, so copy it somewhere safe straight away.
  </Step>
  <Step title="Paste this into the same chat">
    Use the same conversation as before, so it already knows your setup. If that chat is gone, start a new one and paste the first box again before this one.

````markdown paste-2-going-live.md
# Switch my LedgerSync integration to live

Ask me for my live key before you change anything. Do not ask me to type
it into a file you show me, and do not print it back to me. Put it in my
environment settings.

1. Change the base URL to https://api.ledgersyncappv2.com/v3 and use my
   live key on every request from now on.
2. Create the webhook subscription again against live with
   `POST /webhooks/subscriptions`. Subscriptions do not carry over from
   sandbox. The new `signing_secret` comes back once: write it into
   `.env` if you can, otherwise tell me to copy it out of the response
   myself. Never echo it back to me and never log it.
3. If I gave you a redirect address earlier, add it to the live list too.
   Call `GET /settings/redirect-urls` first, add mine to whatever comes
   back, and POST the combined list. Posting without reading first
   deletes my other entries. Sandbox and live keep separate lists.

Nothing about how you read or write data changes between sandbox and live.
````
  </Step>
</Steps>

From here on it works exactly like the practice run, with real banks. You send each client a link, they find their own bank and sign in on our page, and their transactions start arriving in your software. You never see their bank password.

## 4. If something is not working

You do not need to know why. Paste this into the same chat and let it check its own work.

````markdown paste-3-fix-it.md
Check your own code against this list, one line at a time, and fix
anything that does not match.

- Every connection id you saved looks like con_SOURCE_NUMBER, never con_
  followed by a plain uuid.
- client_id is on every accounts, transactions and statements request.
- You branch on error.code, not on the HTTP status or the message text.
- The webhook signature is built from timestamp + "." + the raw body,
  not the body on its own, and you reject timestamps over 5 minutes old.
- You never automatically retry a failed POST.
- download_url is joined to the API host, not the /v3 URL, and sent with
  my key.
- Nothing looks up a Client by external_id or metadata, because both come
  back empty. It uses the cli_ id.

Tell me in plain words what you changed.
````

Still stuck? Email [support@ledgersync.com](mailto:support@ledgersync.com) with what you were doing and we will look.

---

Building this by hand instead? The [Quickstart](/guides/quickstart) covers the same ground for developers.
